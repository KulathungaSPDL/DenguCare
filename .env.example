# Copy this file to .env and fill in real values. Never commit .env.

# Used by a future "read this blood report photo automatically" feature on the
# Reports tab (platelet / haematocrit / WBC extraction from a photographed
# report). Not yet wired up in this build — see app/(tabs)/reports.tsx.
# Get a key from https://console.anthropic.com/
EXPO_PUBLIC_ANTHROPIC_API_KEY=
